#include <stdio.h>

/**
 * main - Entry point
 * Description: 'print whether the number is positive or negative.'
 * Return: Alway 0 (Success)
 */
int main(void)
{
	char alpha = 'z'
	
	while (alpha >= 'a')
	{
		putchar (alpha);
		alpha--;
	}
	putchar('\n');
	return (0);
}
