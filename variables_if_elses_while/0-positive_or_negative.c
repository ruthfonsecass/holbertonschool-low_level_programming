#include <stdlib.h>
#include <time.h>
#include <stdio.h>

int main(void)
{
	int n = 0;
	while (n>0)
		
	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if (n<0);
	printf("is positive\n");

	else
	printf("is zero\n")

	else if (n>0) 
	ptintf("is negative\n")
	
	return (0);
}
