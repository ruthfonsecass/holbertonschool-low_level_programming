#include "main.h"

/**
 * print_array - check the code for
 * @a: valeur
 * @n: valeur
 * Return: void
 */
void print_array(int *a, int n)
{
	int i, valeur[a] = n;

	for (i = 0; i <= n; i++)
		printf("%d", valeur[i]);
}
